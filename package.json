{
  "name": "dy-forklift-notifier",
  "version": "0.1.0",
  "private": true,
  "homepage": "https://BossNick18.github.io/dyforklift",
  "dependencies": {
    "axios": "^1.6.8",
    "firebase": "^9.0.0",
    "gh-pages": "^6.1.1",
    "react": "^17.0.2",
    "react-dom": "^17.0.2",
    "react-router-dom": "^6.23.1",
    "react-scripts": "4.0.3",
    "web-vitals": "^4.0.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "set NODE_OPTIONS=--openssl-legacy-provider && react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "predeploy": "npm run build",
  "deploy": "gh-pages -d build"
    
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
